<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appy_saranya.newfirebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Chat.UserActivity"></activity>
        <activity android:name=".Chat.RegisterActivity" /> <!-- <activity -->
        <!-- android:name=".Chat.ChatActivity" -->
        <!-- android:label="@string/title_activity_chat" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"></activity> -->
        <activity android:name=".ListElementsActivity" />
        <activity android:name=".LoginSample" />
        <activity android:name=".Chat.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Chat.ChatActivity" />
    </application>

</manifest>